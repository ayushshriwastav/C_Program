#include<stdio.h>
int main()
{
    int a, b;
    int sum;
    printf("Enter the first number and second number : ");
    scanf("%d %d", &a, &b);
    sum = a + b;
    printf("the addition of sum = %d \n", sum);
    return 0;
}